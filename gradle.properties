org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
# The IntelliJ platform ships its own Kotlin stdlib; never bundle one.
kotlin.stdlib.default.dependency=false
# Path to an installed IDE to compile against (Rider.app, IntelliJ IDEA.app, ...).
# Leave empty to download Rider from JetBrains instead. Override per machine in local.properties.
gel.localIde=
